# Core FastAPI and dependencies
fastapi==0.68.0
uvicorn==0.15.0

# Database (SQLite only)
sqlalchemy==1.4.23
aiosqlite==0.17.0

# Configuration (older pydantic that doesn't need Rust)
pydantic==1.8.2
python-dotenv==0.19.0
